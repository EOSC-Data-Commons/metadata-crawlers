version: "3.9"

services:
  harvester:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    entrypoint: ["python", "-m", "harvester"]
